spring:
  application:
    name: budget
apollo:
  bootstrap:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
app:
  id: budget

